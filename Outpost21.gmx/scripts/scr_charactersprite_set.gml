///scr_charactersprite_set( race);
//NOTES: this needs to be called using with, 
//it does not return anything, but actively sets skin values

switch argument0 {
    default:
        sprite_index = global.race_mod[ argument0, 4];
    break;
}
